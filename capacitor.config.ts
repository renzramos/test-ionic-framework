import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'test.ionic.ic.framework',
  appName: 'test-ionic-ic-framework',
  webDir: 'www/browser'
};

export default config;
